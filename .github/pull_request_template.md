### Why?
<!-- Describe why this change is being made. Briefly include history and
context, high-level what this PR does, and what the world looks like afterward. -->

### What?
<!--
List out the key changes made in this PR, e.g.
- adds the `retryable` property to `BrontoAPIError`
- updates tests -->

### See Also
<!-- Any linked issues or additional context that help explain this change. -->

## Changelog
<!-- Heads up! This section should include entries for any user-facing changes.
Either fill it out or remove it if there are no entries to report.

List changes that affect end users, e.g.
- Adds `AsyncBrontoClient.datasets.list()`
- Fixes single-slash URL joining for bare `monitors/{id}` paths

List breaking changes first with a ⚠️ prefix, e.g.
- ⚠️ Renames `window_to_ms` to `parse_window`
-->
